{
	"scripts": {
		"start:run-json-server": "json-server -w db.json",
		"start:parcel": "parcel index.html",
		"start": "concurrently npm:start:*"
	},
	"devDependencies": {
		"concurrently": "^4.1.1",
		"json-server": "^0.15.0",
		"parcel-bundler": "^1.12.3",
		"typescript": "^3.5.2"
	},
	"dependencies": {
		"axios": "^0.19.0"
	}
}
